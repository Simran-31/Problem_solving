Statement :Read N arrays, having M elements each, and print the maximum value of each array
CODE:
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
    // Write C++ code here
   int t;
   cin>>t;
   while(t--){
       int m;
       cin>>m;
       int arr[m];
       for(int i=0;i<m;i++){
           cin>>arr[i];
       }
       int maxi=INT_MIN;
       for(int i=0;i<m;i++){
           maxi=max(maxi,arr[i]);
       }
       cout<<maxi<<endl;
   }
    return 0;
}
